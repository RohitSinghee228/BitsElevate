version: '3.8'

services:
  BitsElevate-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: BitsElevate-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - MONGODB_URI=mongodb://mongodb:27017/BitsElevate
      - JWT_SECRET=your_jwt_secret
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - MAILGUN_API_KEY=${MAILGUN_API_KEY}
      - MAILGUN_DOMAIN=${MAILGUN_DOMAIN}
      - MAILGUN_ENABLED=false
    depends_on:
      - mongodb
    networks:
      - BitsElevate-network

  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    networks:
      - BitsElevate-network

networks:
  BitsElevate-network:
    driver: bridge

volumes:
  mongodb-data: 